app:
  environment: test

cloud:
  aws:
    region:
      static: us-east-1
    credentials:
      access-key-id: test
      secret-key: test
    dynamodb:
      endpoint: ${AWS_DYNAMODB_ENDPOINT}
      tables:
        recipients:
          table-name: 'recipients'

client:
  api:
    bank-accounts:
      base-url: 'http://localhost:${wiremock.server.port}'

